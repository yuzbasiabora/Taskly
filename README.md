# Taskly
Taskly is a lightweight single-page to-do app built with plain HTML, CSS, and JavaScript. Users can add, edit, delete, filter, and reorder tasks, track progress with live stats, enjoy confetti on completion, and keep data saved with localStorage without any backend or dependencies.
